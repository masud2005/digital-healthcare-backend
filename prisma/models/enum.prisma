enum Role {
    ADMIN
    DOCTOR
    PATIENT
}

enum UserStatus {
    ACTIVE
    DISABLED
    BLOCKED
    SUSPENDED
    DELETED
}

enum CategoryStatus {
    ACTIVE
    DISABLED
}

enum AssessmentStatus {
    DRAFT
    ACTIVE
    DISABLED
}

enum QuestionType {
    INFORMATION_ONLY
    SINGLE_CHOICE
    MULTIPLE_CHOICE
    INPUT
}

enum Alignment {
    LEFT
    CENTER
    RIGHT
}
