// ENUMS_START

enum Alignment {
  LEFT
  CENTER
  RIGHT
}

enum AssessmentStatus {
  DRAFT
  ACTIVE
  DISABLED
}

enum CategoryStatus {
  ACTIVE
  DISABLED
}

enum OtpPurpose {
  LOGIN
  REGISTER
}

enum QuestionType {
  INFORMATION_ONLY
  SINGLE_CHOICE
  MULTIPLE_CHOICE
  INPUT
}

enum Role {
  ADMIN
  DOCTOR
  PATIENT
}

enum SubmissionStatus {
  PENDING
  ACCEPTED
  REJECTED
}

// ENUMS_END
